import type { Listing } from "@/types/listing";

/**
 * Disabled legacy mock data.
 * Listings now load from Firestore through `lib/listings/firestore-data.ts`.
 */
export const mockListings: Listing[] = [];
